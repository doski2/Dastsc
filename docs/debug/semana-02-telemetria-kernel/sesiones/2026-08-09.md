# Sesión TSC — 2026-08-09

## Contexto

- **Semana**: 02 — Telemetría (kernel)
- **Tren**: Class 323 Cross City (sesión principal); ICE T (freno / gradiente)
- **Perfil activo**: `class323.json` / `icet.json`
- **Ruta / escenario**: Cross City (323); validación unitaria ICE T
- **Rama git**: working tree local

## Entorno

- [x] Backend arrancado (`Dastsc-V3/backend`)
- [x] V4 abierto
- [x] Lua copiado a `RailWorks/plugins/`
- [x] `GetData.txt` con telemetría activa

## Prueba

| Paso   | Esperado                                 | Real                                                                 | OK    |
| ------ | ---------------------------------------- | -------------------------------------------------------------------- | ----- |
| 1      | Kernel tests verdes                      | `npm run test -w @nexus/kernel`                                      | x     |
| 2      | ICE T: `brake.position` = VirtualBrake   | Normalización perfil + tests agente                                  | x     |
| 3      | 323: `brake.combined` negativo al frenar | Confirmado en snapshot V4                                            | x     |
| 4      | Salida estación 0 → 50 km                | `stickyStationDistance` acepta salto                                 | x     |
| 5      | Cabina 1/2/Auto en V4                    | Inferencia Lua + RD + override manual; latch en parada               | x     |
| 6      | Gradiente ICE T (`gradient_sign_flip`)   | `gradient_mode: driver` coherente en UI                              | x     |
| 7      | OCR sin salto +40 m                      | Rechazo en `should_accept_ocr_distance`; `mid_leg_correction` > 5 km | x     |
| 8      | Logs sesión V4                           | `tick`, `ocr_capture`, `backend_tick`, meta perfil                   | x     |

## Telemetría clave

```text
```

## Comandos / ack

```text
```

## Resultado

- [x] OK — semana 2 cerrada
- [ ] KO — ver `issues.md`

## Notas

- Cabina: plugin global sin `OnCameraEnter` fiable; detección por rueda/reverser + latch; override

  V4 si falla.

- OCR: eventos `door_anchor`, `mid_leg_correction` (tramos > 5 km), `near_correction` (≤ 400 m).
- **Siguiente**: `docs/debug/semana-03-backend-comandos/` (SendCommand, perfiles WS) o semana 4–5 si

  priorizas agente.
